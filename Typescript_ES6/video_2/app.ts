let mensaje = "hola";

if(true){
    //Valor solo valido dentro del IF
    let mensaje = "adios";
}

console.log(mensaje);

//Solo lectura
const OPCIONES = "todas";

if(true){
    //Valor solo valido dentro del IF
    const OPCIONES = "ninguna";
}