"use strict";
var mensaje = "hola";
if (true) {
    //Valor solo valido dentro del IF
    var mensaje_1 = "adios";
}
console.log(mensaje);
//Solo lectura
var OPCIONES = "todas";
if (true) {
    //Valor solo valido dentro del IF
    var OPCIONES_1 = "ninguna";
}
